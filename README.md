# 🤖 Telegram-бот для трекинга времени

Профессиональный Telegram-бот для эффективного учета рабочего времени с интеграцией Jira, автоматическим завершением задач и подробной аналитикой производительности.

## ✨ Основные возможности

### 📝 Трекинг задач
- **Автоматический учет времени** - начинайте задачи простым сообщением
- **Гибкие форматы ввода** - поддержка различных способов создания задач
- **Указание времени начала** - возможность задать конкретное время старта
- **Комментарии к задачам** - добавление дополнительной информации

### 🔗 Интеграция с Jira  
- **Распознавание ссылок** - автоматическое извлечение номеров тикетов из URL
- **Поддержка тикетов** - работа с номерами задач в формате PROJ-123
- **Умный парсинг** - определение Jira-задач в любом месте сообщения

### 🌍 Управление временем
- **Часовые пояса** - поддержка любых временных зон мира
- **Настройка рабочего дня** - установка начала и окончания работы
- **Автозавершение задач** - автоматическое завершение по окончании рабочего дня
- **Функция отдыха** - учет перерывов и отдыха

### 📊 Аналитика и отчеты
- **Дневные сводки** - подробный отчет о проделанной работе
- **Группировка задач** - объединение одинаковых задач с общим временем
- **Уникальные комментарии** - показ всех комментариев для каждой задачи
- **Форматированный вывод** - читаемое отображение времени и данных

### 👥 Многопользовательская поддержка
- **Изоляция данных** - у каждого пользователя свои задачи и настройки
- **Персональные настройки** - индивидуальные часовые пояса и рабочие часы
- **Фоновые задачи** - автоматическая обработка для всех пользователей

## 🚀 Быстрый старт

### 📋 Требования

- **Python 3.11+** - основной язык разработки
- **PostgreSQL** - база данных для хранения задач и пользователей  
- **Telegram Bot Token** - токен для работы с Telegram Bot API

### ⚙️ Установка и настройка

#### 1️⃣ Подготовка окружения
```bash
# Клонируйте репозиторий
git clone <repository-url>
cd telegram-time-tracking-bot

# Установите зависимости
pip install python-telegram-bot psycopg2-binary pytz pytest
```

#### 2️⃣ Создание Telegram-бота
1. Найдите [@BotFather](https://t.me/botfather) в Telegram
2. Отправьте команду `/newbot` 
3. Следуйте инструкциям для создания бота
4. Получите токен бота (выглядит как `123456789:ABCDEF...`)

#### 3️⃣ Настройка базы данных
```bash
# Создайте базу данных PostgreSQL
createdb telegram_time_tracker

# Установите переменные окружения
export BOT_TOKEN="ваш_токен_бота"
export DATABASE_URL="postgresql://user:password@localhost:5432/telegram_time_tracker"
```

#### 4️⃣ Инициализация и запуск
```bash
# Создайте таблицы в базе данных
python -c "from database import init_database; init_database()"

# Запустите бота
python main.py
```

### 🎯 Первые шаги после запуска

1. **Найдите вашего бота** в Telegram по имени
2. **Отправьте команду** `/start` для инициализации  
3. **Настройте часовой пояс**: `/set_timezone Europe/Moscow`
4. **Установите рабочие часы**: `/set_workday 09:00 18:00`
5. **Создайте первую задачу**: просто отправьте название задачи

## 📚 Руководство пользователя

### 🎮 Основные команды

| Команда | Описание | Пример |
|---------|----------|--------|
| `/start` | Начать работу с ботом | `/start` |
| `/set_timezone` | Установить часовой пояс | `/set_timezone Europe/Moscow` |
| `/set_workday` | Настроить рабочее время | `/set_workday 09:00 18:00` |

### 🎛️ Интерфейс бота

После запуска вы увидите три основные кнопки:

- **🏖️ Отдых** - Завершить текущую задачу и начать перерыв
- **📊 Сводка** - Показать детальный отчет за текущий день  
- **❓ Помощь** - Полный список команд и инструкций

### ✍️ Форматы создания задач

#### 🎯 Простые задачи
```
Разработка новой функции
Код-ревью для коллеги
Планирование спринта
Документирование API
```

#### ⏰ Задачи с указанием времени
```
14:30 Встреча с командой
09_15 Ежедневный стендап
16:00 Демонстрация заказчику
```

#### 💬 Задачи с комментариями
```
Рефакторинг - улучшение производительности
Тестирование - проверка новых фич
Деплой - релиз версии 2.1.0
```

#### 🔗 Интеграция с Jira
```
https://company.atlassian.net/browse/PROJ-123
https://jira.example.com/browse/TASK-456 - исправление критического бага
PROJ-789 - добавление новых тестов
DEV-1234 - настройка CI/CD
```

### 📈 Примеры использования

#### Типичный рабочий день:

1. **Утром (9:00)**
   ```
   09:00 Проверка почты и планирование дня
   ```

2. **Переключение на задачу (9:30)**
   ```
   Разработка функции авторизации
   ```

3. **Работа с Jira (11:00)**
   ```
   PROJ-123 - исправление бага с валидацией
   ```

4. **Перерыв на обед**
   ```
   Нажимаете кнопку "🏖️ Отдых"
   ```

5. **После обеда (14:00)**
   ```
   14:00 Код-ревью - проверка Pull Request от коллеги
   ```

6. **Проверка прогресса**
   ```
   Нажимаете кнопку "📊 Сводка"
   ```

#### Пример дневного отчета:
```
📊 Сводка за 27 июня 2025

🔄 Текущая задача:
**Код-ревью** (начата в 14:00, выполняется 1 ч 30 мин)

📋 Задачи за день:
• **Проверка почты** — 30 мин
• **Разработка функции авторизации** — 1 ч 30 мин  
• **PROJ-123** — 45 мин
• **🏖️ Отдых** — 1 ч
• **Код-ревью** — 1 ч 30 мин (текущая)

⏱️ Общее рабочее время: 4 ч 15 мин

💬 Комментарии:
• исправление бага с валидацией
• проверка Pull Request от коллеги
```

### ⚙️ Настройки по умолчанию

При первом запуске бот устанавливает:

- **Часовой пояс**: `Europe/Moscow` (UTC+3)  
- **Рабочий день**: `09:00 - 18:00`
- **Автозавершение**: Включено (задачи завершаются в 18:00 или в 23:59 для задач после рабочего дня)

## 🧪 Тестирование

Проект включает полный набор автотестов для обеспечения качества кода:

### Запуск тестов
```bash
# Все тесты
python -m pytest

# Конкретный модуль  
python -m pytest tests/test_task_parser.py

# С покрытием кода
python -m pytest --cov=. --cov-report=html

# Подробный вывод
python -m pytest -v --tb=short
```

### Структура тестов
```
tests/
├── test_models.py         # Тесты моделей User и Task
├── test_task_parser.py    # Тесты парсинга задач и Jira
├── test_time_utils.py     # Тесты работы со временем
└── conftest.py           # Конфигурация тестов и фикстуры
```

### CI/CD Pipeline

Проект настроен для работы с GitHub Actions:
- Автоматическое тестирование при каждом коммите
- Проверка качества кода с Black, isort, flake8, mypy
- Генерация отчетов о покрытии кода
- Поддержка Python 3.11+

## 👥 Для разработчиков

### 🏗️ Архитектура проекта

```
telegram-time-tracking-bot/
├── main.py              # Точка входа и конфигурация бота
├── bot_handlers.py      # Обработчики команд и сообщений
├── models.py           # Модели данных (User, Task)
├── database.py         # Подключение и инициализация БД
├── task_parser.py      # Парсинг задач и Jira-ссылок
├── time_utils.py       # Утилиты для работы со временем
├── tests/              # Автотесты
├── TZ.md              # Техническое задание (80+ требований)
└── README.md          # Документация проекта
```

### 🔧 Технический стек

- **Python 3.11+** - основной язык разработки
- **python-telegram-bot** - библиотека для работы с Telegram Bot API
- **PostgreSQL** - база данных для хранения пользователей и задач
- **psycopg2** - адаптер для работы с PostgreSQL
- **pytz** - библиотека для работы с часовыми поясами
- **pytest** - фреймворк для тестирования
- **GitHub Actions** - CI/CD пайплайн

### 🎯 Ключевые особенности реализации

#### Модульная архитектура
- Разделение ответственности между модулями
- Слабая связанность компонентов
- Высокая когезия логически связанного кода

#### Обработка времени
```python
# Поддержка множества часовых поясов
user.get_local_time(utc_time)  # UTC -> локальное время
user.get_utc_time(local_time)  # локальное время -> UTC

# Автоматическое завершение задач
should_auto_end_task(user, task_start_time)
```

#### Парсинг задач
```python
# Гибкий парсинг различных форматов
parse_task_message("14:30 PROJ-123 - исправление бага")
# Возвращает: ("PROJ-123", "исправление бага", True)

extract_jira_ticket("https://company.atlassian.net/browse/TASK-456")  
# Возвращает: "TASK-456"
```

### 🛠️ Настройка среды разработки

```bash
# Клонирование репозитория
git clone <repository-url>
cd telegram-time-tracking-bot

# Создание виртуального окружения
python -m venv venv
source venv/bin/activate  # Linux/Mac

# Установка зависимостей
pip install python-telegram-bot psycopg2-binary pytz pytest
```

### 📊 База данных

```sql
-- Структура таблиц
CREATE TABLE users (
    user_id BIGINT PRIMARY KEY,
    timezone VARCHAR(50) DEFAULT 'Europe/Moscow',
    workday_start TIME DEFAULT '09:00:00',
    workday_end TIME DEFAULT '18:00:00'
);

CREATE TABLE tasks (
    id SERIAL PRIMARY KEY,
    user_id BIGINT REFERENCES users(user_id),
    task_name VARCHAR(500) NOT NULL,
    comment TEXT,
    start_time TIMESTAMP WITH TIME ZONE NOT NULL,
    end_time TIMESTAMP WITH TIME ZONE,
    is_rest BOOLEAN DEFAULT FALSE
);
```

## 🚀 Развертывание

### Replit Deployment
1. Импортируйте проект в Replit
2. Настройте переменные окружения в Secrets
3. Запустите проект

### Docker (опционально)
```dockerfile
FROM python:3.11-slim
WORKDIR /app
COPY requirements.txt .
RUN pip install -r requirements.txt
COPY . .
CMD ["python", "main.py"]
```

## 📈 Мониторинг и логирование

Бот ведет подробное логирование всех операций:
- Обработка команд пользователей
- Операции с базой данных
- Автоматическое завершение задач
- Ошибки и исключения

## 🤝 Вклад в проект

1. Форкните репозиторий
2. Создайте ветку для новой функции
3. Добавьте тесты для новой функциональности
4. Убедитесь, что все тесты проходят
5. Создайте Pull Request

## 📝 Лицензия

Этот проект распространяется под лицензией MIT.

## 📞 Поддержка

Если у вас возникли вопросы или проблемы:
1. Проверьте существующие Issue в репозитории
2. Создайте новый Issue с подробным описанием проблемы
3. Приложите логи бота и шаги для воспроизведения

---

**Создано с ❤️ для эффективного управления временем**

## Тестирование

### Установка тестовых зависимостей

```bash
pip install pytest pytest-asyncio pytest-mock pytest-cov
```

### Запуск тестов

```bash
# Запуск всех тестов
pytest

# Запуск тестов с покрытием
pytest --cov=. --cov-report=html

# Запуск конкретного модуля
pytest tests/test_time_utils.py -v

# Запуск с подробным выводом
pytest -v --tb=short
```

### Структура тестов

```
tests/
├── __init__.py
├── test_time_utils.py      # Тесты утилит времени
├── test_task_parser.py     # Тесты парсера задач
├── test_models.py          # Тесты моделей данных
└── conftest.py            # Настройки pytest
```

### Покрытие кода

Цель проекта - поддержка покрытия тестами не менее 75%. 

Для просмотра отчета о покрытии:
```bash
pytest --cov=. --cov-report=html
open htmlcov/index.html
```

## CI/CD

Проект настроен для автоматического тестирования в GitHub Actions:

### Конфигурации

- **Tests** (`.github/workflows/tests.yml`) - Запуск тестов с PostgreSQL
- **Code Quality** (`.github/workflows/lint.yml`) - Проверка качества кода

### Автоматические проверки

- ✅ Модульные тесты с pytest
- ✅ Покрытие кода (минимум 75%)
- ✅ Форматирование кода (Black)
- ✅ Сортировка импортов (isort)
- ✅ Стиль кода (flake8)
- ✅ Проверка типов (mypy)

### Локальная проверка перед коммитом

```bash
# Форматирование кода
black .

# Сортировка импортов
isort .

# Проверка стиля
flake8 .

# Запуск тестов
pytest
```

## Архитектура

### Модули

- `main.py` - Точка входа и конфигурация бота
- `bot_handlers.py` - Обработчики команд и сообщений Telegram
- `models.py` - Модели данных (User, Task)
- `database.py` - Подключение и управление базой данных
- `task_parser.py` - Парсинг сообщений и извлечение задач
- `time_utils.py` - Утилиты для работы с временем

### База данных

```sql
-- Пользователи
CREATE TABLE users (
    user_id BIGINT PRIMARY KEY,
    timezone VARCHAR(50) DEFAULT 'Europe/Moscow',
    workday_start TIME DEFAULT '09:00',
    workday_end TIME DEFAULT '18:00',
    created_at TIMESTAMP DEFAULT NOW()
);

-- Задачи
CREATE TABLE tasks (
    id SERIAL PRIMARY KEY,
    user_id BIGINT REFERENCES users(user_id),
    task_name VARCHAR(255) NOT NULL,
    comment TEXT,
    start_time TIMESTAMP NOT NULL,
    end_time TIMESTAMP,
    is_rest BOOLEAN DEFAULT FALSE
);
```

## Развертывание

### Replit

1. Импортируйте проект в Replit
2. Установите переменные окружения в Secrets:
   - `BOT_TOKEN` - токен Telegram бота
   - `DATABASE_URL` - строка подключения к PostgreSQL
3. Запустите проект

### Docker

```dockerfile
FROM python:3.11-slim

WORKDIR /app
COPY requirements.txt .
RUN pip install -r requirements.txt

COPY . .
CMD ["python", "main.py"]
```

## Лицензия

MIT License - см. LICENSE файл для деталей.

## Поддержка

Для вопросов и предложений создавайте Issues в GitHub репозитории.